@using Actris.Abstraction.Model.View
@using Newtonsoft.Json
@model  Actris.Abstraction.Model.View.GridParam

@{
    Model.FilterList.GridId = Model.GridId;
}

<div id="@Model.GridId" class="gridlist">
    @{
        Html.RenderAction("GridList", Model.FilterList);
    }
</div>
@Html.Partial("Component/Grid/AdaptiveFilter/_InitModal", Model)


@*MODAL DELETE CONFIRMATION*@
<div id="ModalDelete" class="modal fade" data-bs-backdrop="static" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content">
            <div class="modal-header ">
                <div class="d-flex">
                    <div>
                        <div class="warning-circle"><i class="fa fa-triangle-exclamation"></i></div>
                    </div>

                    <div>
                        <div class="fw-semibold fs-5 mb-1">Delete Action Tracking</div>
                        <div>Are you sure you want to delete this item?</div>
                        <div class="fw-semibold text-black delete-text-name"></div>
                    </div>
                </div>


            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-danger  w-25" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-danger  w-25" onclick="crud.confirmDelete()">Confirm</button>
            </div>
        </div>
    </div>
</div>