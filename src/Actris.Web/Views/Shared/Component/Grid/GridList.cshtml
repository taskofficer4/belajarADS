@using Actris.Abstraction.Model.View
@using Newtonsoft.Json
@model GridListModel


<div class="filter-container">
    @Html.Partial("Component/Grid/_GridHeader")
    @Html.Partial("Component/Grid/_FilterApplied")
</div>

<div class="table-responsive">
    <table class="table table-hover">
        @Html.Partial("Component/Grid/_THead")
        <tbody>
        @if (Model.FilterList.IsForLookup || Model.HideActionButton)
        {
            @Html.Partial("Component/Grid/GridRows_Lookup")
        }
        else
        {
            @Html.Partial("Component/Grid/GridRows_Crud")
        }
        </tbody>
    </table>
    
    @Html.Partial("Component/Grid/_GridNoResult")
</div>
@Html.Partial("Component/Grid/_GridFooter")